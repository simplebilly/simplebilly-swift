//
// StockMovement.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct StockMovement: Codable, JSONEncodable, Hashable {

    /** Signed movement: positive = into stock, negative = out of stock. */
    public var delta: Int64
    /** One of the `MOVEMENT_*` constants. */
    public var movementType: MovementType
    /** References the product entity. */
    public var productId: UUID
    /** Absolute quantity moved (always >= 0). */
    public var quantity: Int64
    public var reason: String?
    /** Primary-key of the referencing entity. */
    public var referenceId: String?
    /** Entity that caused the movement, e.g. `goods_receipt`, `stock_transfer`. */
    public var referenceType: ReferenceType?
    /** References the warehouse entity. */
    public var warehouseId: String

    public init(delta: Int64, movementType: MovementType, productId: UUID, quantity: Int64, reason: String? = nil, referenceId: String? = nil, referenceType: ReferenceType? = nil, warehouseId: String) {
        self.delta = delta
        self.movementType = movementType
        self.productId = productId
        self.quantity = quantity
        self.reason = reason
        self.referenceId = referenceId
        self.referenceType = referenceType
        self.warehouseId = warehouseId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case delta
        case movementType
        case productId
        case quantity
        case reason
        case referenceId
        case referenceType
        case warehouseId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(delta, forKey: .delta)
        try container.encode(movementType, forKey: .movementType)
        try container.encode(productId, forKey: .productId)
        try container.encode(quantity, forKey: .quantity)
        try container.encodeIfPresent(reason, forKey: .reason)
        try container.encodeIfPresent(referenceId, forKey: .referenceId)
        try container.encodeIfPresent(referenceType, forKey: .referenceType)
        try container.encode(warehouseId, forKey: .warehouseId)
    }
}

