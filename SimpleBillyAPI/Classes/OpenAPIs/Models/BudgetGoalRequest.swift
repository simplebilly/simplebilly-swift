//
// BudgetGoalRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct BudgetGoalRequest: Codable, JSONEncodable, Hashable {

    /** Monthly goal amount (gross). 0 means \"no goal\" (fallback to default). */
    public var monthlyGoal: String
    /** Budget year the goal applies to. */
    public var year: Int

    public init(monthlyGoal: String, year: Int) {
        self.monthlyGoal = monthlyGoal
        self.year = year
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case monthlyGoal = "monthly_goal"
        case year
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(monthlyGoal, forKey: .monthlyGoal)
        try container.encode(year, forKey: .year)
    }
}

