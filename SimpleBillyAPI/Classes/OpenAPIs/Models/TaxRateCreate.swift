//
// TaxRateCreate.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct TaxRateCreate: Codable, JSONEncodable, Hashable {

    /** ISO 3166-1 alpha-2 country code. */
    public var countryCode: String
    /** Date this rate took effect; `None` = not date-bound. */
    public var effectiveFrom: Date?
    /** Default rate for the country (one per country); fallback for lookups when no dated rate applies. */
    public var isDefault: Bool
    /** Human name, e.g. \"VAT\". */
    public var name: String
    /** Rate in hundredths of a percent: 1900 = 19.00%. */
    public var ratePercent: Int64

    public init(countryCode: String, effectiveFrom: Date? = nil, isDefault: Bool, name: String, ratePercent: Int64) {
        self.countryCode = countryCode
        self.effectiveFrom = effectiveFrom
        self.isDefault = isDefault
        self.name = name
        self.ratePercent = ratePercent
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case countryCode
        case effectiveFrom
        case isDefault
        case name
        case ratePercent
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(countryCode, forKey: .countryCode)
        try container.encodeIfPresent(effectiveFrom, forKey: .effectiveFrom)
        try container.encode(isDefault, forKey: .isDefault)
        try container.encode(name, forKey: .name)
        try container.encode(ratePercent, forKey: .ratePercent)
    }
}

