//
// ApiResponseSubscriptionOverviewData.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Tenant subscription overview for the billing page: current plan, status, period end, trial state, effective limits, current usage and feature flags. Backed by Paddle Billing webhook data written into &#x60;billing_info&#x60; + &#x60;tenants.plan&#x60;, and by the canonical plans in &#x60;crate::saasy::plans&#x60;.  JSON contract (camelCase, matches the frontend): &#x60;plan&#x60;, &#x60;planName&#x60;, &#x60;priceEur&#x60;, &#x60;status&#x60;, &#x60;currentPeriodEnd&#x60;, &#x60;manageUrl&#x60;, &#x60;trialEndsAt&#x60;, &#x60;isTrialing&#x60;, &#x60;limits:{maxUsers,maxInvoicesPerMonth,maxConnectors}&#x60;, &#x60;usage:{users,invoicesThisMonth,connectors,overageSeats}&#x60;, &#x60;features:{taxAutomations,fancyReports,erp}&#x60;. */
public struct ApiResponseSubscriptionOverviewData: Codable, JSONEncodable, Hashable {

    public var currentPeriodEnd: Date?
    public var features: PlanFeatures
    public var isTrialing: Bool
    public var limits: PlanLimits
    public var manageUrl: String?
    /** Resolved plan id (free/starter/business/enterprise, or a custom override id). */
    public var plan: String
    public var planName: String
    /** Monthly price in EUR; `-1.0` = custom pricing (enterprise). */
    public var priceEur: Double
    public var quantity: Int?
    public var status: String?
    public var subscriptionId: String?
    public var trialEndsAt: Date?
    public var usage: UsageSnapshot

    public init(currentPeriodEnd: Date? = nil, features: PlanFeatures, isTrialing: Bool, limits: PlanLimits, manageUrl: String? = nil, plan: String, planName: String, priceEur: Double, quantity: Int? = nil, status: String? = nil, subscriptionId: String? = nil, trialEndsAt: Date? = nil, usage: UsageSnapshot) {
        self.currentPeriodEnd = currentPeriodEnd
        self.features = features
        self.isTrialing = isTrialing
        self.limits = limits
        self.manageUrl = manageUrl
        self.plan = plan
        self.planName = planName
        self.priceEur = priceEur
        self.quantity = quantity
        self.status = status
        self.subscriptionId = subscriptionId
        self.trialEndsAt = trialEndsAt
        self.usage = usage
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case currentPeriodEnd
        case features
        case isTrialing
        case limits
        case manageUrl
        case plan
        case planName
        case priceEur
        case quantity
        case status
        case subscriptionId
        case trialEndsAt
        case usage
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(currentPeriodEnd, forKey: .currentPeriodEnd)
        try container.encode(features, forKey: .features)
        try container.encode(isTrialing, forKey: .isTrialing)
        try container.encode(limits, forKey: .limits)
        try container.encodeIfPresent(manageUrl, forKey: .manageUrl)
        try container.encode(plan, forKey: .plan)
        try container.encode(planName, forKey: .planName)
        try container.encode(priceEur, forKey: .priceEur)
        try container.encodeIfPresent(quantity, forKey: .quantity)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(subscriptionId, forKey: .subscriptionId)
        try container.encodeIfPresent(trialEndsAt, forKey: .trialEndsAt)
        try container.encode(usage, forKey: .usage)
    }
}

