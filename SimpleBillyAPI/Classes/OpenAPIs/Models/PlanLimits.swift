//
// PlanLimits.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Per-plan numeric limits. &#x60;-1&#x60; in any field means unlimited. */
public struct PlanLimits: Codable, JSONEncodable, Hashable {

    public var maxConnectors: Int
    public var maxInvoicesPerMonth: Int64
    public var maxUsers: Int
    public var metered: [String: Int64]?
    /** Connectors that are *not* included in this plan (require a higher tier). Empty = all connectors included on this plan. */
    public var paidConnectors: [String]

    public init(maxConnectors: Int, maxInvoicesPerMonth: Int64, maxUsers: Int, metered: [String: Int64]? = nil, paidConnectors: [String]) {
        self.maxConnectors = maxConnectors
        self.maxInvoicesPerMonth = maxInvoicesPerMonth
        self.maxUsers = maxUsers
        self.metered = metered
        self.paidConnectors = paidConnectors
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case maxConnectors
        case maxInvoicesPerMonth
        case maxUsers
        case metered
        case paidConnectors
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(maxConnectors, forKey: .maxConnectors)
        try container.encode(maxInvoicesPerMonth, forKey: .maxInvoicesPerMonth)
        try container.encode(maxUsers, forKey: .maxUsers)
        try container.encodeIfPresent(metered, forKey: .metered)
        try container.encode(paidConnectors, forKey: .paidConnectors)
    }
}

