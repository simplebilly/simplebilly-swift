//
// OffenlegungReport.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct OffenlegungReport: Codable, JSONEncodable, Hashable {

    /** Fristende (Abschlussstichtag + Frist). */
    public var deadline: Date
    /** Offenlegungsfrist in Monaten (§ 325 Abs. 4 HGB). */
    public var deadlineMonths: Int
    public var items: [OffenlegungItem]
    /** Annahme über die Kapitalmarktorientierung. */
    public var kapitalmarktorientiert: Bool
    public var note: String
    /** Berichtsjahr (laufendes Kalenderjahr). */
    public var year: Int

    public init(deadline: Date, deadlineMonths: Int, items: [OffenlegungItem], kapitalmarktorientiert: Bool, note: String, year: Int) {
        self.deadline = deadline
        self.deadlineMonths = deadlineMonths
        self.items = items
        self.kapitalmarktorientiert = kapitalmarktorientiert
        self.note = note
        self.year = year
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case deadline
        case deadlineMonths = "deadline_months"
        case items
        case kapitalmarktorientiert
        case note
        case year
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(deadline, forKey: .deadline)
        try container.encode(deadlineMonths, forKey: .deadlineMonths)
        try container.encode(items, forKey: .items)
        try container.encode(kapitalmarktorientiert, forKey: .kapitalmarktorientiert)
        try container.encode(note, forKey: .note)
        try container.encode(year, forKey: .year)
    }
}

