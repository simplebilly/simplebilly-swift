//
// SalesVolumeReport.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct SalesVolumeReport: Codable, JSONEncodable, Hashable {

    public var grandTotal: String
    public var items: [SalesVolumeItem]
    public var totalCount: Int64

    public init(grandTotal: String, items: [SalesVolumeItem], totalCount: Int64) {
        self.grandTotal = grandTotal
        self.items = items
        self.totalCount = totalCount
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case grandTotal = "grand_total"
        case items
        case totalCount = "total_count"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(grandTotal, forKey: .grandTotal)
        try container.encode(items, forKey: .items)
        try container.encode(totalCount, forKey: .totalCount)
    }
}

