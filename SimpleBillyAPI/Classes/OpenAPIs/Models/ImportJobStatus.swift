//
// ImportJobStatus.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ImportJobStatus: Codable, JSONEncodable, Hashable {

    public static let processedRule = NumericRule<Int64>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let progressRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let totalRule = NumericRule<Int64>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    /** Set only when the job failed. */
    public var error: String?
    public var jobId: String
    public var processed: Int64
    /** 0–100 */
    public var progress: Int
    /** Which competitor the import came from (lexoffice | billbee); the frontend uses it to label the job. Absent for legacy jobs. */
    public var provider: String?
    /** queued | fetching | downloading | importing | done */
    public var stage: String
    /** pending | running | done | failed */
    public var status: String
    public var total: Int64

    public init(error: String? = nil, jobId: String, processed: Int64, progress: Int, provider: String? = nil, stage: String, status: String, total: Int64) {
        self.error = error
        self.jobId = jobId
        self.processed = processed
        self.progress = progress
        self.provider = provider
        self.stage = stage
        self.status = status
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case error
        case jobId = "job_id"
        case processed
        case progress
        case provider
        case stage
        case status
        case total
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(error, forKey: .error)
        try container.encode(jobId, forKey: .jobId)
        try container.encode(processed, forKey: .processed)
        try container.encode(progress, forKey: .progress)
        try container.encodeIfPresent(provider, forKey: .provider)
        try container.encode(stage, forKey: .stage)
        try container.encode(status, forKey: .status)
        try container.encode(total, forKey: .total)
    }
}

