//
// SubmitResultDto.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct SubmitResultDto: Codable, JSONEncodable, Hashable {

    /** Selected answer indices (required for scored builtin trainings). */
    public var answers: [Int]
    public var assignmentId: UUID?
    /** Score 0–100. Only trusted for plugin trainings without server-side scoring; builtin trainings are always re-scored from `answers`. */
    public var score: Int
    public var trainingCode: String

    public init(answers: [Int], assignmentId: UUID? = nil, score: Int, trainingCode: String) {
        self.answers = answers
        self.assignmentId = assignmentId
        self.score = score
        self.trainingCode = trainingCode
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case answers
        case assignmentId
        case score
        case trainingCode
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(answers, forKey: .answers)
        try container.encodeIfPresent(assignmentId, forKey: .assignmentId)
        try container.encode(score, forKey: .score)
        try container.encode(trainingCode, forKey: .trainingCode)
    }
}

