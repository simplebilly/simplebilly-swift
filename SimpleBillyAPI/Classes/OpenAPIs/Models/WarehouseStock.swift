//
// WarehouseStock.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct WarehouseStock: Codable, JSONEncodable, Hashable {

    /** Batch/lot number (Chargennummer) — `None` for non-batched goods. */
    public var batchNumber: String?
    public var binLocation: String?
    /** Expiry date for batch-tracked goods. */
    public var expiryDate: Date?
    public var productId: UUID
    public var quantity: Int64
    /** JSON array of serial numbers (Seriennummern) in this stock row. */
    public var serialNumbers: AnyCodable?
    public var warehouseId: String

    public init(batchNumber: String? = nil, binLocation: String? = nil, expiryDate: Date? = nil, productId: UUID, quantity: Int64, serialNumbers: AnyCodable? = nil, warehouseId: String) {
        self.batchNumber = batchNumber
        self.binLocation = binLocation
        self.expiryDate = expiryDate
        self.productId = productId
        self.quantity = quantity
        self.serialNumbers = serialNumbers
        self.warehouseId = warehouseId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case batchNumber
        case binLocation
        case expiryDate
        case productId
        case quantity
        case serialNumbers
        case warehouseId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(batchNumber, forKey: .batchNumber)
        try container.encodeIfPresent(binLocation, forKey: .binLocation)
        try container.encodeIfPresent(expiryDate, forKey: .expiryDate)
        try container.encode(productId, forKey: .productId)
        try container.encode(quantity, forKey: .quantity)
        try container.encodeIfPresent(serialNumbers, forKey: .serialNumbers)
        try container.encode(warehouseId, forKey: .warehouseId)
    }
}

