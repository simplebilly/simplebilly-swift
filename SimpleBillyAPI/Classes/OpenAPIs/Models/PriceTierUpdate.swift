//
// PriceTierUpdate.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct PriceTierUpdate: Codable, JSONEncodable, Hashable {

    /** None = tier applies to all customers; otherwise a customer group id. */
    public var customerGroupId: String?
    /** Quantity from which this tier applies (inclusive). */
    public var minQuantity: Int64?
    /** References the product entity. */
    public var productId: UUID?
    /** Net unit price once `min_quantity` is reached. */
    public var unitPrice: String

    public init(customerGroupId: String? = nil, minQuantity: Int64? = nil, productId: UUID? = nil, unitPrice: String) {
        self.customerGroupId = customerGroupId
        self.minQuantity = minQuantity
        self.productId = productId
        self.unitPrice = unitPrice
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case customerGroupId
        case minQuantity
        case productId
        case unitPrice
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(customerGroupId, forKey: .customerGroupId)
        try container.encodeIfPresent(minQuantity, forKey: .minQuantity)
        try container.encodeIfPresent(productId, forKey: .productId)
        try container.encode(unitPrice, forKey: .unitPrice)
    }
}

